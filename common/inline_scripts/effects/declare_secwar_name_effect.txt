limit = { event_target:sec_rebel_planet = { check_variable = { which = sec_reb_number value = $NUM$ } } }
set_name = {
	key = NAME_sec_rebellion_war_$NUM$
	variable_string = "[sector_name.GetName]"
	### Even this this is inside a scripted effect, the above cant use the preceding \\'s or else it breaks
}
create_leader = {
	class = random_ruler
	species = event_target:civwar_species
	name = random
	skill = 5
	background_ethic = ethic_$ETHIC$
	background_planet = event_target:civil_war_planet
	effect = {
		set_leader_flag = new_civwar_ruler
		save_event_target_as = new_rebel_leader
		# if = {
		# 	limit = {
		# 		leader_class = official
		# 		NOT = { has_global_flag = has_ALTM_mod }
		# 	}
		# 	add_trait = {
		# 		trait = trait_ruler_deep_connections_2
		# 		show_message = no
		# 	}
		# }
		# else = {
		# 	add_trait = {
		# 		trait = leader_trait_deep_connections_2
		# 		show_message = no
		# 	}
		# }
	}
}
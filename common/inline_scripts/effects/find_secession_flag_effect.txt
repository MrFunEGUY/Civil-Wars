limit = { has_country_flag = secession_country_$NUM$_flag }
if = {
	limit = { exists = event_target:civwar_rebels }
	event_target:civwar_rebels = {
		set_country_flag = secession_country_$NUM$_seceder
		if = {
			limit = { exists = ruler }
			ruler = { set_leader_flag = secession_country_$NUM$_traitor }
		}
	}
}
else_if = {
	limit = { has_origin = origin_exile_government }
	save_global_event_target_as = secession_country_$NUM$
}

if = {
	limit = { any_country = { has_country_flag = secession_country_$NUM$_seceder } }
	set_country_flag = country_needed_for_loc
}